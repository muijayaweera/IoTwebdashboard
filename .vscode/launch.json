{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Smart Dashboard",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8000/smart.html",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Run Local Server"
        }
    ]
}
